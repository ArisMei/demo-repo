git clone 
git status
git add git. txt // git add .
git commit -m "a" -m "some description"
git push orgin

#Start Locally the repository
git init //Turn into Git Repository
git remote add origin
git remote -v

git push origin master
git push -v origin master
